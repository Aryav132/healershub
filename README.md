
  # Homepage and Device Pages

  This is a code bundle for Homepage and Device Pages. The original project is available at https://www.figma.com/design/JDmjh4qxNwV9x9skVgo6nc/Homepage-and-Device-Pages.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  